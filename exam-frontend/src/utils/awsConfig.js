// // Frontend AWS Configuration
// const AWS_CONFIG = {
//     region: import.meta.env.VITE_AWS_REGION || 'ap-south-1',
//     s3Bucket: import.meta.env.VITE_S3_BUCKET_NAME || 'exam-system-files-prachi',
    
// };

// export default AWS_CONFIG;
// Frontend AWS Configuration
const AWS_CONFIG = {
    region: import.meta.env.VITE_AWS_REGION || 'ap-south-1',
    s3Bucket: import.meta.env.VITE_S3_BUCKET_NAME || 'exam-system-files-prachi',
};

export default AWS_CONFIG;